# ApiTest.txt — Pseudocod (Arrange – Act – Assert)

Arrange:
  - env: dev (din config/app.yaml)
  - baseUrl: https://jsonplaceholder.typicode.com
  - endpoint: /todos/1
  - timeouts: connect 5000ms, read 5000ms

Act:
  - Trimite un request HTTP GET către: {baseUrl}{endpoint}

Assert:
  - Verifică status code == 200
  - Verifică răspunsul conține câmpul 'title' nenul (string ne-gol)

Note:
  - Acesta este un plan de test (pseudocod). În această temă NU rulăm un test Java real.